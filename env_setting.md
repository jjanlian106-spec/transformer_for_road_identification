# 1. 安装好conda，将conda加入环境变量
# 2. conda -V 查看安装的conda版本，看是否成功过安装conda
# 3. 直接在 power shell 下执行： conda create -n 虚拟环境名 python = python版本号
# 4. 激活虚拟环境：conda activate 虚拟环境名
# 5. 在虚拟环境下安装pytorch等依赖库：pip3 install 依赖库